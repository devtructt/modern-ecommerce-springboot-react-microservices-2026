import { createTheme } from '@mui/material/styles';

const theme = createTheme({
/*   typography: {
    // Tells MUI the user's browser is using a 16px baseline
    htmlFontSize: 16, 
    // Shrinks the standard text multiplier from 14 down to 12
    fontSize: 12
  },
  components: {
    MuiSvgIcon: {
      styleOverrides: {
        fontSizeSmall: { fontSize: '1.4286rem' },
        fontSizeMedium: { fontSize: '1.5rem' },
        fontSizeLarge: { fontSize: '2.5rem' },
      },
    },
  }, */
});

export default theme;
